name = "chomp-urls"
main = "worker-themed-no-titles.js"
compatibility_date = "2024-01-20"

# KV namespace for storing short URLs
[[kv_namespaces]]
binding = "URLS"
id = "477d2e500d5841aabd0c7ce842c49a0e"

# Environment variables
# These are non-sensitive configuration values
# Secrets (ADMIN_PASSWORD, API_KEY) are set via GitHub Actions
[vars]
